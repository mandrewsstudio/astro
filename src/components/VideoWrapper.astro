---
export interface Props {
  src: string;
  alt?: string;
  playsinline?: boolean;
}
const { src, alt = '', playsinline = true } = Astro.props;
---
<video autoplay muted loop {playsinline}>
  <source src={src} type="video/mp4" />
  {alt}
</video>
