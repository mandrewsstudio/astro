<nav>
        <!-- Keep the same structure as the original site so CSS and scripts work the same way.
             - .hero-section contains the logo (SVG or external image).
             - .title matches existing CSS that positions the site title.
        -->
        <section class="hero-section">
         <a href="/" style="border:none;"> <img src="/images/logo.svg" alt="In Progress logo" class="Layer_2 logo" /></a>
        </section>



        <div>
          
          <a href="/work/">Work</a> 
          <a href="/about/">Studio</a> 
          <a href="https://instagram.com/inprogress_studio" target="_blank">Follow</a> 
          <a href="/">In Progress</a>
        </div>
      </nav>
      <!-- Load client-side scripts in order and only in the browser. -->
  <script type="module">
  // Load only logo-rotate.js
  (async () => {
    try {
      const s = document.createElement('script');
      s.src = '/js/logo-rotate.js';
      s.onload = () => console.log('logo-rotate loaded');
      s.onerror = () => console.warn('Failed to load logo-rotate');
      document.head.appendChild(s);
    } catch (e) {
      console.warn('Script load error:', e);
    }
  })();
</script>